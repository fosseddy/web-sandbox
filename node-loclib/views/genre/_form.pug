form(method="POST")
    div
        if form.errors.name
            ul: li= form.errors.name

        input(placeholder="Genre name...", name="name" value=form.name)

    button(type="submit") Submit
